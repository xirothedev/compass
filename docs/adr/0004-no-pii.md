# Store only SBD + scores, no PII

SBD + scores without names are published openly by the press and the Ministry supports lookup by SBD, so the risk is low. Full names + birth dates are personal data under Decree 13/2023, so never ingest/display them, never offer public bulk downloads; add rate limits + a deletion-request button and record the source of every row.
